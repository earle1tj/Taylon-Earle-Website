import { NextResponse } from "next/server";
import { getStudioUser } from "../../../studio-auth";
import { cleanInput, deletePost, updatePost } from "../../../journal/journalStore";
async function identity(params:Promise<{id:string}>) { const {id}=await params; const value=Number(id); return Number.isSafeInteger(value)&&value>0?value:null; }
export async function PUT(request:Request,{params}:{params:Promise<{id:string}>}) { const user=await getStudioUser(); if(!user)return NextResponse.json({error:"Sign in is required."},{status:401}); try { const id=await identity(params); if(!id)return NextResponse.json({error:"Invalid entry."},{status:400}); const post=await updatePost(id,user.email,cleanInput(await request.json())); if(!post)return NextResponse.json({error:"Entry not found."},{status:404}); return NextResponse.json({post}); } catch(error) { const message=error instanceof Error?error.message:"The entry could not be saved."; const duplicate=message.includes("Duplicate entry"); return NextResponse.json({error:duplicate?"That journal URL is already in use.":message},{status:duplicate?409:400}); } }
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}) { const user=await getStudioUser(); if(!user)return NextResponse.json({error:"Sign in is required."},{status:401}); const id=await identity(params); if(!id)return NextResponse.json({error:"Invalid entry."},{status:400}); const deleted=await deletePost(id,user.email); if(!deleted)return NextResponse.json({error:"Entry not found."},{status:404}); return NextResponse.json({deleted}); }